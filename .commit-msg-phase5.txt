Phase 5: validation suite, deploy bundle, DNS cutover, archive PR

Validation
- server/engines/__tests__/persona-smoke.test.ts — five-persona smoke
  suite (Member-formational, Member-relational, Advisor-wealth,
  Manager-audit, Admin-CI) all green via the cross-engine chatRouter.
- docs/phase5/VALIDATION_CHECKLIST.md — pre-cutover gate covering
  build integrity, tests, migration dry-run, staging migration,
  smoke against staging, deploy bundle, and authorization.

Deploy
- Existing multi-stage Dockerfile reused (verified non-root, tini PID-1,
  /healthz HEALTHCHECK, ~180 MB image).
- docs/phase5/PHASE_5_RUNBOOK.md — pre-flight gates, build, Phase 3
  apply against prod, parallel-hostname stand-up, DNS cutover (TTL
  drop → CNAME flip → propagation watch → metrics watch), seven-day
  watch window with daily checks, and the archival PR sequence.

DNS / Redirect
- docs/phase5/dns/Caddyfile.legacy-redirect — drop-in 301 redirect
  for stewardly-ai.app → stewardly.app and a 308 for the API host.

Archival
- docs/phase5/STEWARDLY_AI_ARCHIVE_PR.md — full draft of the PR that
  will be opened against mwpenn94/stewardly-ai (ARCHIVED.md, README
  redirect, .github/repo-config.yml, branch-protection removal,
  operator post-merge steps, rollback plan). The single destructive
  action — clicking GitHub's Archive button — is intentionally
  excluded and gated by Mike's manual confirmation.

Verification
- `pnpm check` exits 0
- `pnpm test server/engines/` 9/9 tests passing
