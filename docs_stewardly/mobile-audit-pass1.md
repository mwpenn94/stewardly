# Mobile Audit Pass 1 — Key Findings

## Billing Page (mobile)
- Layout: Clean, centered content with sign-in prompt
- Header: AppLayout header visible (hamburger + logo + theme + notifications) — correct
- Bottom nav: Visible with Home/Tasks/Billing/More — Billing is highlighted correctly
- FeedbackWidget FAB: Visible above bottom nav — GOOD
- Issue: FeedbackWidget FAB appears to be overlapping with the bottom nav slightly — the white circle is right at the edge

## Memory Page (mobile)  
- Layout: Clean, centered sign-in prompt
- Header: Correct
- Bottom nav: Visible and correct
- FeedbackWidget: Same slight overlap concern

## Common Issues Across Pages
1. FeedbackWidget FAB position needs fine-tuning — it's very close to the bottom nav
2. All non-Home pages correctly show the AppLayout header on mobile
3. Bottom nav is properly visible on all pages except Home (where it should also be visible)
