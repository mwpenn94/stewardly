Phase 2 - Five-engine architecture and Wealth mission port

Adds server/engines/_intent.ts with the Intent contract: Intent, IntentMeta,
IntentResult, SubstrateRouter, EngineContext, AdminLevel, and the
assertComplianceAdminAllowed guard.

Adds server/engines/_substrate.ts with the eight substrate primitives
(chat-surface, agentic-runtime, rag, embeddings, voice,
document-intelligence, classifier, proposal-generator) and the Stewardship
aggregate (brand language, counsel-review flags, pricing parameters, EMBA
guard).

Adds server/engines/_substrateAdapters.ts: defaultSubstrate, the local
classifier (always local; gates everything; sub-cent estimate before any
provider call), and a SubstrateRouter that enforces:
 - compliance-class intents refuse adminLevel automatic
 - EMBA Section 7 boundary (onlinelearning.quantic.edu) is refused
 - audit row written for every dispatch

Adds server/engines/index.ts: chatRouter (the only legal cross-engine path)
and the engine registry.

Scaffolds five engines:
 - formational (was Learning)
 - relational  (was People)
 - missional   (was Wealth, promoted to a generic engine; wealth is one
                mission alongside pastoral, teaching, healthcare, coaching,
                community)
 - contextual  (was Data)
 - continuous-improvement (name retained)

Ports stewardly-ai wealth functionality into engines/missional/wealth/:
 - calculators-core (pure-math wealth engine, 46 calculators, 14 product
   models, copied from packages/manus-next/wealth-engine)
 - wealth-engines-legacy (UWE, BIE, HE, SCUI server engines)
 - calculators.ts: missional.wealth.calculate handler
 - planning.ts: missional.wealth.plan composes core into a plan and a
   costed proposal
 - advisor.ts: missional.wealth.recommend (compliance class,
   counsel-review-gated)
 - household.ts: missional.wealth.household.graph
 - ce.ts: missional.wealth.ce.track (compliance class)

Scaffolds pastoral, teaching, healthcare, coaching, community mission
specializations on the same Intent contract.

Adds 4 vitest cases covering engine routing, wealth.calculate, EMBA
Section 7 refusal, and the compliance-class admin-level guard. The full
project tsc --noEmit passes (zero errors). The new engine tests pass.

References: STEWARDLY v3 sections 3, 4, and 7.
