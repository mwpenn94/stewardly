Phase 4: integrate glass components + apply 'clearly appropriate' test

Glass components (client/src/components/glass/)
- 5 top-level: ChatGreeting, AppsGridMenu, VoiceOrb, PersonaSidebar5,
  design-tokens.css
- 9 substrate: ActionIndicator, ATLASGoalPanel, ConnectionQualityIndicator,
  MemoryInsightPanel, QualityScoreDisplay, SearchCascadePanel,
  SovereignModeIndicator, TierBadge, WorkspaceArtifactsPanel
- README.md preserved alongside the components

Foundation glue
- contexts/DisclosureContext.tsx — 0–3 progressive-disclosure level with
  localStorage persistence; wired into App.tsx as DisclosureProvider.
- hooks/useNavBadges.ts — Map<route, BadgeInfo> hook with formatBadgeCount
  helper; stub returns empty Map until engines emit nav events.
- App.tsx — wraps the tree with <DisclosureProvider>.
- New peer deps: @radix-ui/react-visually-hidden, react-i18next, i18next.

'Clearly appropriate' test
- docs/phase4/CLEARLY_APPROPRIATE_TEST.md — 16-row rationale log applying
  the three-condition test to every stewardly-ai infrastructure candidate.
  Outcome: 11 INTEGRATE, 3 STUB (aiToolsRegistry, Gmail, pass-artifacts),
  2 DROP (webapp-builds, manus-bridge), 1 DEFER (addendum-phaseN tests).

Verification
- `npx tsc --noEmit` passes with zero errors after every change.
- All 14 glass components compile against the foundation's existing CSS
  tokens — no design-tokens.css double-import needed.
