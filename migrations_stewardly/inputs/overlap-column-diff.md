# Stewardly v3 overlap column reconciliation

Generated by `migrations/scripts/02_diff_overlap_columns.py`. Per
STEWARDLY v3 §6 the migration is **additive only** — overlap-table
columns from the foundation are never dropped, and overlap-table
columns from stewardly-ai are added as new columns alongside.

## `users` overlap reconciliation

- Columns in both: **9** — kept as-is.
- Columns only in stewardly-ai: **32** — added via ALTER TABLE.
- Columns only in foundation: **2** — preserved (never dropped).

### Added (`ALTER TABLE ... ADD COLUMN`)
- `affiliateOrgId` (TS: `affiliateOrgId`)
- `anonymousConversationCount` (TS: `anonymousConversationCount`)
- `authTier` (TS: `authTier`)
- `auth_provider` (TS: `authProvider`)
- `avatarUrl` (TS: `avatarUrl`)
- `employer_name` (TS: `employerName`)
- `google_address_json` (TS: `googleAddressJson`)
- `google_birthday` (TS: `googleBirthday`)
- `google_gender` (TS: `googleGender`)
- `google_id` (TS: `googleId`)
- `google_organizations_json` (TS: `googleOrganizationsJson`)
- `google_phone` (TS: `googlePhone`)
- `job_title` (TS: `jobTitle`)
- `linkedin_headline` (TS: `linkedinHeadline`)
- `linkedin_id` (TS: `linkedinId`)
- `linkedin_industry` (TS: `linkedinIndustry`)
- `linkedin_location` (TS: `linkedinLocation`)
- `linkedin_profile_url` (TS: `linkedinProfileUrl`)
- `passwordHash` (TS: `passwordHash`)
- `profile_enriched_at` (TS: `profileEnrichedAt`)
- `profile_enrichment_source` (TS: `profileEnrichmentSource`)
- `settings` (TS: `settings`)
- `sign_in_data_json` (TS: `signInDataJson`)
- `stripe_current_period_end` (TS: `stripeCurrentPeriodEnd`)
- `stripe_customer_id` (TS: `stripeCustomerId`)
- `stripe_plan_id` (TS: `stripePlanId`)
- `stripe_subscription_id` (TS: `stripeSubscriptionId`)
- `stripe_subscription_status` (TS: `stripeSubscriptionStatus`)
- `styleProfile` (TS: `styleProfile`)
- `suitabilityCompleted` (TS: `suitabilityCompleted`)
- `suitabilityData` (TS: `suitabilityData`)
- `tosAcceptedAt` (TS: `tosAcceptedAt`)

### Preserved (foundation-only)
- `stripeCustomerId` (TS: `stripeCustomerId`)
- `stripeSubscriptionId` (TS: `stripeSubscriptionId`)


## `tasks` overlap reconciliation

- Columns in both: **4** — kept as-is.
- Columns only in stewardly-ai: **13** — added via ALTER TABLE.
- Columns only in foundation: **18** — preserved (never dropped).

### Added (`ALTER TABLE ... ADD COLUMN`)
- `assigned_to` (TS: `assignedTo`)
- `category` (TS: `category`)
- `client_id` (TS: `clientId`)
- `completed_at` (TS: `completedAt`)
- `created_at` (TS: `createdAt`)
- `description` (TS: `description`)
- `due_date` (TS: `dueDate`)
- `recurring` (TS: `recurring`)
- `recurring_interval` (TS: `recurringInterval`)
- `related_entity_id` (TS: `relatedEntityId`)
- `related_entity_type` (TS: `relatedEntityType`)
- `updated_at` (TS: `updatedAt`)
- `user_id` (TS: `userId`)

### Preserved (foundation-only)
- `archived` (TS: `archived`)
- `completedSteps` (TS: `completedSteps`)
- `createdAt` (TS: `createdAt`)
- `currentStep` (TS: `currentStep`)
- `externalId` (TS: `externalId`)
- `favorite` (TS: `favorite`)
- `maxRetries` (TS: `maxRetries`)
- `projectId` (TS: `projectId`)
- `retryCount` (TS: `retryCount`)
- `staleCompleted` (TS: `staleCompleted`)
- `systemPrompt` (TS: `systemPrompt`)
- `taskRecursiveOptDepth` (TS: `taskRecursiveOptDepth`)
- `taskRecursiveOptEnabled` (TS: `taskRecursiveOptEnabled`)
- `timeoutSeconds` (TS: `timeoutSeconds`)
- `totalSteps` (TS: `totalSteps`)
- `updatedAt` (TS: `updatedAt`)
- `userId` (TS: `userId`)
- `workspaceUrl` (TS: `workspaceUrl`)


## `user_preferences` overlap reconciliation

- Columns in both: **4** — kept as-is.
- Columns only in stewardly-ai: **34** — added via ALTER TABLE.
- Columns only in foundation: **11** — preserved (never dropped).

### Added (`ALTER TABLE ... ADD COLUMN`)
- `autoFollowUp` (TS: `autoFollowUp`)
- `autoFollowUpCount` (TS: `autoFollowUpCount`)
- `autoGenerateCharts` (TS: `autoGenerateCharts`)
- `autoPlayVoice` (TS: `autoPlayVoice`)
- `chatDensity` (TS: `chatDensity`)
- `citationStyle` (TS: `citationStyle`)
- `communicationStyle` (TS: `communicationStyle`)
- `contextDepth` (TS: `contextDepth`)
- `creativity` (TS: `creativity`)
- `crossModelVerify` (TS: `crossModelVerify`)
- `customPromptAdditions` (TS: `customPromptAdditions`)
- `customShortcuts` (TS: `customShortcuts`)
- `disclaimerVerbosity` (TS: `disclaimerVerbosity`)
- `discoveryContinuous` (TS: `discoveryContinuous`)
- `discoveryDirection` (TS: `discoveryDirection`)
- `discoveryIdleThresholdMs` (TS: `discoveryIdleThresholdMs`)
- `ensembleWeights` (TS: `ensembleWeights`)
- `financialGoals` (TS: `financialGoals`)
- `focusModeDefaults` (TS: `focusModeDefaults`)
- `handsFreeMode` (TS: `handsFreeMode`)
- `language` (TS: `language`)
- `maxTokens` (TS: `maxTokens`)
- `modelPreferences` (TS: `modelPreferences`)
- `reasoningTransparency` (TS: `reasoningTransparency`)
- `responseFormat` (TS: `responseFormat`)
- `responseLength` (TS: `responseLength`)
- `riskTolerance` (TS: `riskTolerance`)
- `sidebarDefault` (TS: `sidebarDefault`)
- `stateOfResidence` (TS: `stateOfResidence`)
- `taxFilingStatus` (TS: `taxFilingStatus`)
- `temperature` (TS: `temperature`)
- `theme` (TS: `theme`)
- `thinkingDepth` (TS: `thinkingDepth`)
- `ttsVoice` (TS: `ttsVoice`)

### Preserved (foundation-only)
- `capabilities` (TS: `capabilities`)
- `codespaceScopeGranted` (TS: `codespaceScopeGranted`)
- `generalSettings` (TS: `generalSettings`)
- `previewTier` (TS: `previewTier`)
- `recursiveOptimizationDepth` (TS: `recursiveOptimizationDepth`)
- `recursiveOptimizationEnabled` (TS: `recursiveOptimizationEnabled`)
- `recursiveOptimizationTemperature` (TS: `recursiveOptimizationTemperature`)
- `searchConfig` (TS: `searchConfig`)
- `systemPrompt` (TS: `systemPrompt`)
- `vercelProjectId` (TS: `vercelProjectId`)
- `vercelTeamSlug` (TS: `vercelTeamSlug`)

